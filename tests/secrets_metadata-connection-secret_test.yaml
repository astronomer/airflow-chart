---
suite: Test templates/secrets/metadata-connection-secret.yaml
templates:
  - templates/secrets/metadata-connection-secret.yaml
tests:
  - it: should work
    asserts:
      - isKind:
          of: Secret
