########################
## git-sync-relay pvc ##
########################
{{/* PVC is created as a Helm hook in git-sync-relay-init-job.yaml (pre-install, weight 1)
     to ensure it exists before the init job runs. It persists across upgrades. */}}
